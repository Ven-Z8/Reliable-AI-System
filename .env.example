RAP_ENV=development
RAP_LOG_LEVEL=INFO
RAP_RUNS_DIR=.agent-runs
RAP_DATA_DIR=.data
OTEL_SERVICE_NAME=reliable-agent-platform
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Provider and service credentials are introduced only by the milestone
# that needs them. Never commit populated values.
